# Setup
1. `npm install`
2. `npx ganache-cli` (new terminal tab) 
3. `npx oz deploy`
    - `regular`
    - `development`
    - `Mandelbrot`

# Run
`npx oz call`
    - `development`
    - `Mandelbrot at ...`
    - `generateMandelbrot()`
